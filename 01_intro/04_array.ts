const superHeros: string[] = []
// const heroPower: number[] = []
const heroPower: Array<number> = []
superHeros.push("spiderman")
heroPower.push(2)

type User = {
    name: string,
    isActive: boolean
}
const allUsers : User[] = []
allUsers.push({name: "s", isActive: false})

const mlModels: number[][] = [
    [255, 255, 255],
    []
]

export {}
